{
	"$schema": "https://turbo.build/schema.json",
	"extends": ["//"],
	"tasks": {
		"build": {
			"inputs": ["src/**/*.ts", "public/**", "package.json", "tsup.config.ts", "tsconfig.json"],
			"outputs": ["dist/**"]
		}
	}
}
